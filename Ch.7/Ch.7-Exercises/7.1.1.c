// Give the decimal value of each of the following integer constants.

#include <stdio.h>

int main() {

    // (a) 077
    printf("(a): %d\n", 077);

    // (b) 0x77
    printf("(b): %d\n", 0x77);

    // (c) 0XABC
    printf("(c): %d\n", 0XABC);

    return 0;
}
